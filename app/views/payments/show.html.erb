<div class="cart-box-payment">
  <h2 class="cart-title"> 游海收银台 </h2>
   <div class="payment-hint">订单提交成功,请尽快付款   订单号:<%= @order.id %></div>
  <hr>
  <div class="payment-body">
    <h1>支付订单</h1>
    <h3>应付金额 ：<%= @order.total %> 元</h3>
    <br>
    <%= link_to("支付宝支付", pay_with_alipay_order_path(@order.token), :method => :post, :class => "btn btn-info") %>
    <%= link_to("微信支付", pay_with_wechat_order_path(@order.token), :method => :post, :class => "btn btn-success") %>
  </div>
</div>

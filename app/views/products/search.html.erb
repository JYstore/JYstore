<div>
  <br><br>
  <% if @query_string.blank? %>
    <h2 class="search-info-title">
      对不起，搜索内容不能为空，请输入游戏名称再进行搜索。
    </h2>
  <% elsif @products.blank? %>
    <h2 class="search-info-title">找不到您要的游戏</h2>
  <% else %>
    <h2 class="search-info-title"> 有关"<%= @query_string %>"的游戏信息 </h2>
    <div class="bigcard">
      <div class="cardname">
       商品列表
      </div>
      <div class="row">
        <% @products.each do |product| %>
          <div class="col-xs-12 col-xs-3">
            <%= link_to product_path(product), class: "product-path" do %>
            <div class="fathercard">
            <div class="card">
              <% if product.image.present? %>
                <%= image_tag(product.image.url, class: "thumbnail") %>
              <% else %>
                <%= image_tag("http://placehold.it/200x200&text=No Pic", class: "thumbnail") %>
              <% end %>
              <ol>
                <div class="title">
                   <%= product.title %>
                </div>
              </ol>
              <ol>
                <div class="description">
                    <%= product.description %>
                </div>
              </ol>
                <div class="price">
                   ￥<%= product.price %>.00
                </div>
            </div>
            </div>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
    <% if @products.present? %>
    <div class="text-center">
      <%= will_paginate(@products, renderer: BootstrapPagination::Rails) %>
    </div>
    <% end %>
  <% end %>
</div>
